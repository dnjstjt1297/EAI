<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="10 seconds">

 <timestamp key="BY_DATE" datePattern="yyyy-MM-dd"/>
 <property name="LOG_PATH" value="./logs"/>

 <logger name="org.quartz.core.QuartzScheduler" level="WARN"/>
 <logger name="org.quartz.impl.StdSchedulerFactory" level="WARN"/>

 <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
  <encoder>
   <pattern>
    %d{HH:mm:ss.SSS} %highlight(%-5level)-%-100msg [%X{request_id}] [%X{client_ip}] [%logger{20}:%thread]%n
   </pattern>
  </encoder>
 </appender>

 <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
  <file>${LOG_PATH}/${BY_DATE}.log</file>

  <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
   <fileNamePattern>${LOG_PATH}/archive/app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
   <maxFileSize>10MB</maxFileSize>
   <maxHistory>30</maxHistory>
   <totalSizeCap>3GB</totalSizeCap>
  </rollingPolicy>

  <encoder>
   <pattern>
    %d{HH:mm:ss.SSS} %-5level-%-100msg [%X{request_id}] [%X{client_ip}] [%logger{20}:%thread]%n
   </pattern>
  </encoder>
 </appender>

 <root level="INFO">
  <appender-ref ref="STDOUT"/>
  <appender-ref ref="FILE"/>
 </root>


</configuration>